server:
  port: 0

spring:
  application:
    name: FRAUD-SERVICE
  datasource:
    url: jdbc:h2:mem:fauddb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: SA
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

eureka:
  client:
    enabled: false

jwt:
  secret: smart_attendance_fraud_detection_jwt_secret_key_32bytes_minimum
